// Centralized configuration defaults
export const DEFAULT_FLINK_GATEWAY_URL = process.env.FLINK_GATEWAY_URL || 'http://localhost:8083';

// Other central constants can be added here in future (timeouts, retries, etc.)
